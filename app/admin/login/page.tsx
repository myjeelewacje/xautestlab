"use client";import {useState} from "react";import {useRouter} from "next/navigation";
export default function Login(){const [error,setError]=useState("");const router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const data=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch("/api/admin/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(data)});if(r.ok)router.push("/admin");else setError("Invalid password.")}return <main className="legal"><a className="brand" href="/">XAU<span>TestLab</span></a><form className="panel form" onSubmit={submit} style={{marginTop:50}}><span className="eyebrow">Private area</span><h1 style={{fontSize:40}}>Admin login</h1><label className="field"><span>Admin password</span><input type="password" name="password" required autoFocus/></label><button className="button" style={{marginTop:20}}>Sign in</button>{error&&<p className="status">{error}</p>}</form></main>}
