"use client";import {useState} from "react";
export function AdminActions(){const [msg,setMsg]=useState("");async function act(action:string){if(action==="reset"&&!confirm("Permanently delete all poll votes?"))return;const r=await fetch("/api/admin",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action})});const j=await r.json();setMsg(j.message||j.error);if(r.ok)location.reload()}return <>{msg&&<p className="status">{msg}</p>}<div className="admin-help"><button className="button ghost" onClick={()=>location.assign("/api/admin/export")}>Export registrations CSV</button><button className="danger" onClick={()=>act("reset")}>Reset poll test data</button></div></>}
export function QuestionAdmin({id,answer,published}:{id:string;answer:string;published:boolean}){const [text,setText]=useState(answer||"");async function act(action:string){await fetch("/api/admin",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action,id,answer:text})});location.reload()}return <div className="admin-actions"><textarea value={text} onChange={e=>setText(e.target.value)} placeholder="Write an answer…"/><button onClick={()=>act("approve")}>Approve</button><button onClick={()=>act("publish")}>{published?"Update":"Publish answer"}</button><button onClick={()=>act("hide")}>Hide</button><button className="danger" onClick={()=>act("delete-question")}>Delete</button></div>}
export function RegistrationAdmin({id,contacted}:{id:string;contacted:boolean}){async function act(action:string){await fetch("/api/admin",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action,id})});location.reload()}return <div className="admin-actions"><button onClick={()=>act("contacted")}>{contacted?"Contacted ✓":"Mark contacted"}</button><button className="danger" onClick={()=>act("delete-registration")}>Delete</button></div>}
